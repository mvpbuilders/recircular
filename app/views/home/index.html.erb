<div class="container py-3 ">
  <!-- Hero -->
  <!-- Botones de categorías chicos centrados -->
  <div class="d-none d-md-block bg-white py-3 border-bottom mt-1">
    <div class="container d-flex flex-wrap justify-content-center gap-2">
      <% botones = [
        ["ropa", "Ropa", "remera.png"],
        ["muebles", "Muebles/Deco", "sillon.png"],
        ["niños", "Niños", "baby.png"],
        ["accesorios", "Accesorios", "accesorios.png"],
        ["otros", "Otros", "otros.png"]
      ] %>

      <% botones.each do |tipo, label, icon| %>
        <%= link_to explore_tipo_path(tipo: tipo), class: "btn btn-outline-dark btn-sm d-flex align-items-center px-3 py-1" do %>
          <%= image_tag icon, alt: label, width: 16, class: "me-2" %> <%= label %>
        <% end %>
      <% end %>

      <%= link_to owner_products_path(owner: "Caro"), class: "btn btn-outline-dark btn-sm d-flex align-items-center px-3 py-1" do %>
        <%= image_tag "fashion.png", alt: "By Caro", width: 16, class: "me-2" %> By Caro
      <% end %>
    </div>
  </div>

  <!-- Banner -->
<section class="position-relative overflow-hidden" style="background-image: url(<%= asset_path("banner1.png") %>); background-size: cover; background-position: center top; height: 500px;">
  <div class="position-absolute top-0 start-0 w-100 h-100" style="background: rgba(0,0,0,0.25);"></div>

  <div class="container h-100 position-relative d-flex flex-column-reverse flex-md-row align-items-center justify-content-center justify-content-md-start z-1">
    <div class="bg-dark bg-opacity-50 rounded-4 p-4 text-white text-center text-md-start mb-4 mb-md-0" style="max-width: 90%;">
      <h1 class="fw-bold m-0" style="font-size: 2rem; line-height: 1.3;">Comprá usado, comprá increíble</h1>
      <p class="mb-0 mt-2" style="font-size: 1rem;">Productos únicos, al mejor precio</p>
      <%= link_to "#mejores-prendas", class: "btn btn-light fw-semibold mt-3 px-4 py-2" do %>
        Shop Now
      <% end %>
    </div>
  </div>
</section>





  <%# Secciones por categoría %>
  <% {
    "Mejores prendas" => @best_clothing,
    "Mejores muebles" => @best_furniture,
    "Mejores productos Niños" => @best_kids,
    "Mejores uniformes" => @best_uniforms,
    "Mejores otros" => @best_others
  }.each do |title, collection| %>
    <div class="text-center mt-5 mb-4">
      <h4 class="fw-bold text-uppercase mb-2"><%= title %></h4>
      <div style="width: 40px; height: 3px; background-color: #ccc; margin: 0 auto;"></div>
    </div>
    <div class="product-scroll-container mb-5">
      <% collection.each do |product| %>
        <div class="product-card-wrapper">
          <%= render "products/product_card", product: product %>
        </div>
      <% end %>
    </div>
  <% end %>


  <!-- FAQs -->
  <div class="row">
    <div class="col-12">
      <h2 class="section-title text-center mb-5">Preguntas frecuentes</h2>
      <div class="accordion" id="faqAccordion">
        <% [
          ["❓ ¿Qué es Recircular?", "Es una plataforma para comprar y vender cosas usadas de manera simple y confiable."],
          ["📦 ¿Cuáles son los tiempos de entrega?", "Entre 3 y 7 días hábiles según tu localidad."],
          ["💸 ¿Cuánto cuesta el envío?", "Estamos bonificando el envío durante los primeros meses."],
          ["🧾 ¿Qué métodos de pago aceptan?", "Tarjetas, transferencias y Mercado Pago."],
          ["🔄 ¿Cómo devuelvo un producto?", "Tenés 10 días para iniciar devolución sin costo."],
          ["✅ ¿Cómo sé si mi pedido fue exitoso?", "Recibís un email de confirmación. Revisá spam si no lo encontrás."]
        ].each_with_index do |(question, answer), i| %>
          <div class="accordion-item">
            <h2 class="accordion-header" id="faq<%= i %>">
              <button class="accordion-button <%= i > 0 ? 'collapsed' : '' %>" type="button" data-bs-toggle="collapse" data-bs-target="#collapse<%= i %>">
                <%= question %>
              </button>
            </h2>
            <div id="collapse<%= i %>" class="accordion-collapse collapse <%= i == 0 ? 'show' : '' %>">
              <div class="accordion-body"><%= answer %></div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
